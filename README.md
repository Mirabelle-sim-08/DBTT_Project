
  # User dashboard prototype

  This is a code bundle for User dashboard prototype. The original project is available at https://www.figma.com/design/1n7wrgrZWCx2Av4dkephBA/User-dashboard-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  